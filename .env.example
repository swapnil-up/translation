GEMINI_API_KEY=your_key_here

# Local pgvector (Unix socket, peer auth)
DATABASE_URL=postgresql://swap@/nepali_copilot

# Docker or remote (TCP with password)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nepali_copilot

# Production (Supabase/Neon) — swap when ready
# DATABASE_URL=postgresql://user:pass@host:5432/db
